//foreach example
var arr = [1,2,3,4,5,6];
forEach(arr, function(number) {
    console.log(number * 2);
});
//returns

//definition of forEach
function forEach(array, callbackFunction) {
    //things to be implemented
}

//these are the callback parameters for forEach
function callbackFunction(curElement, currentIndex, array) {
    //implemented by caller of forEach
}
